Your screenshot confirms two things:
	1.	Chrome is sharing the location with the tab (location icon is solid, browser permission is granted).
	2.	Your app still says “Loading GPS Map…” and shows 0 MPH, 0.0 Miles, and the map never appears.
	3.	No activity/usage in Mapbox dashboard.

This is classic for one of these two reasons:

⸻

1. GPS Is “Allowed” But Not Resolving

Even though Chrome shows “location is allowed,” sometimes on localhost, VPNs, or cloud dev environments, the Geolocation API call never returns actual coordinates (especially if the browser can’t get your real device IP or the Replit sandbox restricts the API).
	•	Check the browser’s JS console (F12) for errors/warnings like:
	•	“Geolocation error: Timeout expired”
	•	“Only secure origins are allowed”
	•	“User denied Geolocation” (shouldn’t show if allowed)
	•	Or any uncaught error in the GPS/map logic

⸻

2. App Code Is “Stuck” Waiting for GPS

The frontend code probably does:

navigator.geolocation.getCurrentPosition(successCallback, errorCallback)

If the callback never fires (browser stuck), your code will never execute the logic to initialize Mapbox.

⸻

🔥 Quick Debug: FORCE Mapbox to Load

Here’s how to prove your Mapbox code is fine (and see activity on your dashboard):

1. Edit your frontend map component (e.g., Map.tsx or wherever Mapbox is initialized).

Replace the GPS usage with a hardcoded fallback:

const defaultCoords = { lat: 40.7128, lng: -74.0060 }; // NYC

// Instead of waiting for GPS:
const coords = defaultCoords;
mapboxgl.accessToken = import.meta.env.VITE_MAPBOX_TOKEN;
const map = new mapboxgl.Map({
  container: 'map', // Your map div id
  style: 'mapbox://styles/mapbox/streets-v11',
  center: [coords.lng, coords.lat],
  zoom: 13
});

Comment out/remove all code that waits for or uses navigator.geolocation.

⸻

2. Save and restart the frontend.

3. Refresh your app.
	•	The map should load in NYC instantly!
	•	You’ll see usage on your Mapbox dashboard

⸻

If this works:
	•	Your Mapbox integration and token are 100% correct.
	•	The only issue is the environment’s inability to resolve browser GPS.

⸻

If you want help with the code edit, just copy-paste the relevant part and I’ll show you exactly what to change.

⸻

TL;DR:
Your Mapbox code is not the problem. The browser environment is not providing usable GPS coordinates, even though permission is granted.
Force the code to use a static location to prove it—and see the map load!

Want the exact code for your project? Paste the file where the map is initialized!